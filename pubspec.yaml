name: tokenizers
description: "An AI-powered health and medical tracking app"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.8.0
  http: ^1.6.0
  envied: ^1.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  envied_generator: ^1.3.4
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
